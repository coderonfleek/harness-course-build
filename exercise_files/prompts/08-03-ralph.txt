I want to build a Flask REST API for a small blog. Requirements: user registration and JWT auth, CRUD endpoints for posts (title, body, author, created_at), CRUD endpoints for comments (nested under posts, with body and author), input validation on all endpoints, proper error handling with structured JSON responses, SQLAlchemy models with a SQLite database, requirements.txt with pinned versions, and a pytest test suite covering the happy paths for each endpoint plus at least three edge cases (unauthorized access, invalid input, missing resource). Include a README with setup and usage instructions. Before writing any code, draw up a detailed plan in plan.md — one task per meaningful piece of work — so I can review it. Don't start executing yet.